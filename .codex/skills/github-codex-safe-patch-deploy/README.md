# Usage

Use `SKILL.md` as the operating procedure, `CHECKLIST.md` as the execution gate, and `PROMPT_TEMPLATE.md` when starting a new Codex task.

This skill is repository-scoped. Always combine it with the current `PROJECT_CONTEXT.md` and latest base branch.
